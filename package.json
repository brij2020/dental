{
  "name": "dcms-dental-clinic",
  "version": "1.0.0",
  "description": "Dental Clinic Management System - Full Stack Application",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "patient"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend & npm run dev --workspace=patient",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:patient": "npm run dev --workspace=patient",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend && npm run build --workspace=patient",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:patient": "npm run build --workspace=patient",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=patient",
    "preview": "npm run preview --workspace=frontend",
    "watch:backend": "npm run watch --workspace=backend",
    "install-all": "npm install && npm install --workspace=frontend && npm install --workspace=backend && npm install --workspace=patient"
  },
  "keywords": [
    "dental",
    "clinic",
    "management",
    "system"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "winston": "^3.19.0"
  }
}
