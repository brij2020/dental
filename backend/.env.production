# Production Environment Variables
# CRITICAL: Keep this file secure and never commit to git
# Use environment variables in deployment platform instead

# Environment
NODE_ENV=production

# Server Configuration
PORT=8080

# Database Configuration
# MUST BE SET VIA DEPLOYMENT PLATFORM
MONGODB_URI=
DB_NAME=dcms_prod

# JWT Configuration
# MUST BE SET VIA DEPLOYMENT PLATFORM - Use a strong random string
JWT_SECRET=

# CORS Configuration
# Update with your production domain
CORS_ORIGIN=https://dentalclinic.com

# API Configuration
API_URL=https://api.dentalclinic.com
FRONTEND_URL=https://dentalclinic.com

# Logging
LOG_LEVEL=warn

# Email Configuration
# MUST BE SET VIA DEPLOYMENT PLATFORM
EMAIL_USER=
EMAIL_PASS=

# Security Settings
DEBUG=false
REQUEST_TIMEOUT=30000
NODE_OPTIONS=--max-old-space-size=4096
